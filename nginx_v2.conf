events {
    worker_connections 100;
}

http {
    upstream demo-app-v2 {
        server demo-api-v2:80;
    }

    server {
        listen 8000;

        location / {
            proxy_pass http://demo-app-v2;
        }
    }
}
